#!/bin/bash
# DockUp: The Zero-Bloat PaaS CLI
# Usage: 
#   ./dockup setup user@vps-ip   -> Run once to install Docker/Agent on VPS
#   ./dockup init user@vps-ip    -> Run inside a git repo to deploy it

set -e
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
BLUE='\033[0;34m'
NC='\033[0m'

REMOTE=$2
AGENT_SRC="main.go" # Assumes source is in the same folder, or adjust path
BINARY_NAME="dockup-agent"

# --- Helper: Embedded Server Installer ---
# This generates the installer script dynamically so you don't need to manage two files.
generate_installer_script() {
cat << 'EOF' > _remote_install.sh
#!/bin/bash
set -e
echo ">> Installing Dependencies..."
apt-get update -qq && apt-get install -y -qq git jq curl

if ! command -v docker &> /dev/null; then curl -fsSL https://get.docker.com | sh; fi

echo ">> Setting up Directories..."
mkdir -p /opt/dockup/apps /etc/dockup
if [ ! -f /etc/dockup/registry.json ]; then echo "{}" > /etc/dockup/registry.json; fi

echo ">> Installing Agent..."
mv dockup-agent /usr/local/bin/
chmod +x /usr/local/bin/dockup-agent

echo ">> Configuring Systemd..."
cat <<EOT > /etc/systemd/system/dockup.service
[Unit]
Description=DockUp Agent
After=network.target docker.service

[Service]
ExecStart=/usr/local/bin/dockup-agent -port 8080 -config /etc/dockup/registry.json
Restart=always
User=root

[Install]
WantedBy=multi-user.target
EOT

systemctl daemon-reload
systemctl enable --now dockup

echo ">> Generating SSH Key for GitHub..."
if [ ! -f ~/.ssh/id_ed25519 ]; then ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519 -N "" -q; fi
echo "--------------------------------------------------"
echo "IMPORTANT: Add this Deploy Key to your GitHub Repo:"
cat ~/.ssh/id_ed25519.pub
echo "--------------------------------------------------"
EOF
}

# --- Command: SETUP (Provision VPS) ---
cmd_setup() {
    if [ -z "$REMOTE" ]; then echo -e "${RED}Usage: ./dockup setup user@host${NC}"; exit 1; fi

    echo -e "${GREEN}🔨 Building Agent for Linux...${NC}"
    if [ -f "$AGENT_SRC" ]; then
        env GOOS=linux GOARCH=amd64 go build -o $BINARY_NAME $AGENT_SRC
    elif [ ! -f "$BINARY_NAME" ]; then
        echo -e "${RED}Error: Could not find '$AGENT_SRC' or a compiled '$BINARY_NAME'.${NC}"
        exit 1
    fi

    echo -e "${GREEN}🚀 Uploading Agent and Installer to $REMOTE...${NC}"
    generate_installer_script # Create temp script
    scp -q $BINARY_NAME _remote_install.sh $REMOTE:~/

    echo -e "${GREEN}⚙️  Running Remote Installer...${NC}"
    ssh $REMOTE "chmod +x _remote_install.sh && ./_remote_install.sh"
    
    # Cleanup
    rm _remote_install.sh $BINARY_NAME
    echo ""
    echo -e "${GREEN}✅ VPS Setup Complete!${NC}"
    echo ""
    echo -e "${YELLOW}📋 Important Next Steps:${NC}"
    echo ""
    echo -e "${YELLOW}1. Add the SSH Deploy Key to your GitHub repositories:${NC}"
    echo "   The public key was displayed above. Copy it and add it to:"
    echo "   https://github.com/YOUR_USERNAME/YOUR_REPO/settings/keys"
    echo ""
    echo -e "${YELLOW}2. Make sure your VPS firewall allows port 8080:${NC}"
    echo "   The agent listens on port 8080 for webhooks"
    echo ""
    echo -e "${YELLOW}3. Run 'init' command from your project directory:${NC}"
    echo "   cd /path/to/your-project"
    echo "   ./dockup init $REMOTE"
    echo ""
}

# --- Command: INIT (Register Repo) ---
cmd_init() {
    if [ -z "$REMOTE" ]; then echo -e "${RED}Usage: ./dockup init user@host${NC}"; exit 1; fi

    # 1. Context Detection - Must be run from a Git repository
    if ! git rev-parse --show-toplevel > /dev/null 2>&1; then
        echo -e "${RED}❌ Error: Not a Git repository${NC}"
        echo -e "${YELLOW}The 'init' command must be run from inside your project's Git repository.${NC}"
        echo -e "${YELLOW}Example: cd /path/to/your-app && ./dockup init user@vps-ip${NC}"
        exit 1
    fi

    APP_NAME=$(basename `git rev-parse --show-toplevel`)
    REPO_URL=$(git config --get remote.origin.url)
    
    if [ -z "$REPO_URL" ]; then
        echo -e "${RED}❌ Error: No Git remote found${NC}"
        echo -e "${YELLOW}Please add a remote origin to your repository:${NC}"
        echo -e "${YELLOW}  git remote add origin <your-repo-url>${NC}"
        exit 1
    fi
    
    BRANCH=$(git rev-parse --abbrev-ref HEAD)
    SECRET=$(openssl rand -hex 20)

    echo -e "${GREEN}🚀 Initializing '$APP_NAME' on $REMOTE...${NC}"
    echo ""

    # 2. Remote Provisioning
    echo -e "${BLUE}📦 Setting up on VPS...${NC}"
    ssh $REMOTE "
      if [ ! -d '/opt/dockup/apps/$APP_NAME' ]; then
        echo '>> Cloning repository...'
        if ! git clone $REPO_URL /opt/dockup/apps/$APP_NAME 2>&1; then
          echo 'ERROR: Failed to clone repository. Make sure:'
          echo '  1. The Deploy Key is added to your GitHub repository'
          echo '  2. The repository URL is correct: $REPO_URL'
          exit 1
        fi
      else
        echo '>> Repository already exists, skipping clone...'
      fi
      
      echo '>> Registering app in DockUp...'
      tmp=\$(mktemp)
      jq '.\"$APP_NAME\" = {path: \"/opt/dockup/apps/$APP_NAME\", branch: \"$BRANCH\", secret: \"$SECRET\"}' /etc/dockup/registry.json > \$tmp && mv \$tmp /etc/dockup/registry.json
      
      echo '>> Restarting DockUp agent...'
      systemctl restart dockup
    " || {
        echo -e "${RED}❌ Failed to setup on VPS${NC}"
        exit 1
    }

    # 3. GitHub Webhook Setup
    VPS_IP="${REMOTE#*@}"  # Extract IP from user@ip
    WEBHOOK_URL="http://${VPS_IP}:8080/webhook/github"
    
    # Extract repo name from various GitHub URL formats
    # Handles: git@github.com:user/repo.git, https://github.com/user/repo.git, etc.
    REPO_FULL_NAME=$(echo "$REPO_URL" | sed -E 's|.*github\.com[:/]([^/]+/[^/]+?)(\.git)?/?$|\1|' | sed 's|\.git$||')
    
    echo ""
    echo -e "${GREEN}✅ App registered successfully!${NC}"
    echo ""
    echo -e "${YELLOW}📋 Next Steps:${NC}"
    echo ""
    
    if [ -n "$REPO_FULL_NAME" ] && echo "$REPO_FULL_NAME" | grep -q '/'; then
        echo -e "${YELLOW}1. Add GitHub Deploy Key (if not already done):${NC}"
        echo "   https://github.com/$REPO_FULL_NAME/settings/keys"
        echo "   Add the SSH key shown during 'setup' command"
        echo ""
        echo -e "${YELLOW}2. Add GitHub Webhook:${NC}"
        echo "   https://github.com/$REPO_FULL_NAME/settings/hooks"
        echo "   Click 'Add webhook' and enter:"
    else
        echo -e "${YELLOW}1. Add GitHub Deploy Key (if not already done):${NC}"
        echo "   Go to your repository Settings → Deploy keys"
        echo "   Add the SSH key shown during 'setup' command"
        echo ""
        echo -e "${YELLOW}2. Add GitHub Webhook:${NC}"
        echo "   Go to your repository Settings → Webhooks"
        echo "   Click 'Add webhook' and enter:"
    fi
    
    echo ""
    echo -e "   ${BLUE}Payload URL:${NC}    $WEBHOOK_URL"
    echo -e "   ${BLUE}Content type:${NC}   application/json"
    echo -e "   ${BLUE}Secret:${NC}         $SECRET"
    echo -e "   ${BLUE}Events:${NC}         Just the push event"
    echo ""
    
    # Try to add webhook via GitHub API if gh CLI is available and authenticated
    if command -v gh &> /dev/null && [ -n "$REPO_FULL_NAME" ] && echo "$REPO_FULL_NAME" | grep -q '/'; then
        if gh auth status &> /dev/null; then
            echo -e "${GREEN}🔗 Attempting to add webhook via GitHub API...${NC}"
            
            # Use gh api to create webhook
            if gh api "repos/$REPO_FULL_NAME/hooks" \
                -X POST \
                -f name=web \
                -f active=true \
                -f "config[url]=$WEBHOOK_URL" \
                -f "config[content_type]=json" \
                -f "config[secret]=$SECRET" \
                -f "events[]=push" &> /dev/null; then
                echo -e "${GREEN}✅ Webhook added automatically!${NC}"
                echo ""
                echo -e "${GREEN}🎉 Setup Complete! Push to '$BRANCH' to deploy.${NC}"
                echo ""
                return
            else
                echo -e "${YELLOW}⚠️  Could not add webhook automatically.${NC}"
                echo -e "${YELLOW}   Please add it manually using the instructions above.${NC}"
            fi
        else
            echo -e "${YELLOW}💡 Tip: Run 'gh auth login' to enable automatic webhook setup.${NC}"
        fi
    else
        if [ -z "$REPO_FULL_NAME" ] || ! echo "$REPO_FULL_NAME" | grep -q '/'; then
            echo -e "${YELLOW}💡 Could not detect GitHub repository. Please add webhook manually.${NC}"
        elif ! command -v gh &> /dev/null; then
            echo -e "${YELLOW}💡 Tip: Install GitHub CLI ('gh') and run 'gh auth login' for automatic webhook setup.${NC}"
        fi
    fi
    
    echo ""
    echo -e "${GREEN}🎉 Once webhook is added, push to '$BRANCH' to deploy!${NC}"
    echo ""
}

# --- Main Switch ---
case "$1" in
    setup) cmd_setup ;;
    init)  cmd_init ;;
    *)     echo "Usage: ./dockup {setup|init} user@host" ;;
esac

